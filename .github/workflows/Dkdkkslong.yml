name: 🚀 LongVPS 

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 340
    
    steps:
      # ---------------------------
      # BANNER CHÀO MỪNG
      # ---------------------------
      - name: 🎯 KHỞ ĐỘNG HỆ THỐNG
        run: |
          Write-Host ""
          Write-Host ""
          Write-Host "🤖 Longday SERVER" -ForegroundColor Yellow
          Write-Host "⚡ Powered by AIL" -ForegroundColor Green
          Write-Host "🕒 Thời gian: ${{ github.event.inputs.duration }}" -ForegroundColor Magenta
          Write-Host ""
          
          $frames = @('⣾','⣽','⣻','⢿','⡿','⣟','⣯','⣷')
          for($i=0; $i -lt 10; $i++){
              Write-Host "`r🚀 Đang khởi tạo hệ thống... $($frames[$i % 8])" -NoNewline -ForegroundColor Blue
              Start-Sleep -Milliseconds 100
          }
          Write-Host "`r✅ Hệ thống đã sẵn sàng!                    " -ForegroundColor Green

      # ---------------------------
      # CẤU HÌNH HỆ THỐNG
      # ---------------------------
      - name: 🔧 CẤU HÌNH HỆ THỐNG
        run: |
          Write-Host ""
          Write-Host "🛠️  ĐANG CẤU HÌNH HỆ THỐNG" -ForegroundColor Yellow
          
          $steps=@(
              @{Name="Bật Remote Desktop"; Script={Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force}},
              @{Name="Tắt xác thực mạng"; Script={Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force}},
              @{Name="Cấu hình bảo mật RDP"; Script={Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force; Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force}},
              @{Name="Mở port 3389 firewall"; Script={netsh advfirewall firewall add rule name="RDP-Premium" dir=in action=allow protocol=TCP localport=3389 profile=any; netsh advfirewall firewall add rule name="RDP-Premium-Out" dir=out action=allow protocol=TCP localport=3389 profile=any}},
              @{Name="Khởi động dịch vụ"; Script={Set-Service -Name TermService -StartupType Automatic -ErrorAction SilentlyContinue; Set-Service -Name UmRdpService -StartupType Automatic -ErrorAction SilentlyContinue; Start-Service -Name TermService -ErrorAction SilentlyContinue; Start-Service -Name UmRdpService -ErrorAction SilentlyContinue}}
          )

          foreach($step in $steps){
              Write-Host "│ 📋 $($step.Name)..." -NoNewline -ForegroundColor White
              try{
                  & $step.Script
                  Write-Host "`r│ ✅ $($step.Name)" -ForegroundColor Green
              } catch{
                  Write-Host "`r│ ⚠️  $($step.Name) - Đã bỏ qua lỗi nhỏ" -ForegroundColor Yellow
              }
              Start-Sleep -Milliseconds 500
          }
          Write-Host "🎯 Cấu hình hoàn tất!" -ForegroundColor Green

      # ---------------------------
      # WALLPAPER AN TOÀN
      # ---------------------------
      - name: 🖼️ Set RDP Wallpaper
        shell: powershell
        run: |
          Write-Host "🔄 Tải và đổi hình nền RDP an toàn..." -ForegroundColor Cyan

          $url = "https://raw.githubusercontent.com/truongcongdanh542013-art/-i/232a55d5596db3f6f78d22423e2e7353e13dca2c/DF651AEC-04F3-401D-899D-E1D540A1B488.jpeg"
          $path = "C:\Users\Public\wallpaper.jpg"

          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

          try{
              Invoke-WebRequest -Uri $url -OutFile $path -UseBasicParsing -ErrorAction Stop
              Write-Host "✅ Tải ảnh thành công: $path" -ForegroundColor Green
          } catch{
              Write-Host "❌ Không tải được ảnh từ URL: $url" -ForegroundColor Red
          }

          try{
              Add-Type @"
              using System.Runtime.InteropServices;
              public class Wallpaper {
                  [DllImport("user32.dll", SetLastError = true)]
                  public static extern bool SystemParametersInfo(int uAction, int uParam, string lpvParam, int fuWinIni);
              }
"@
              [Wallpaper]::SystemParametersInfo(20, 0, $path, 3)
              Write-Host "✅ Wallpaper đã set xong cho session hiện tại" -ForegroundColor Green
          } catch{
              Write-Host "⚠️ Không thể áp dụng ngay wallpaper, sẽ hiển thị khi user đăng nhập" -ForegroundColor Yellow
          }

      # ---------------------------
      # BOOST MẠNG
      # ---------------------------
      - name: 🚀 BOOST MẠNG – Unlock Bandwidth
        run: |
          Write-Host ""
          Write-Host "🔧 Unlock 100% bandwidth (NonBestEffortLimit -> 0)" -ForegroundColor Yellow
          reg add "HKLM\SOFTWARE\Policies\Microsoft\Windows\Psched" /v "NonBestEffortLimit" /t REG_DWORD /d 0 /f
          Write-Host "✅ Đã unlock bandwidth" -ForegroundColor Green

      # ---------------------------
      # TẠO USER PREMIUM
      # ---------------------------
      - name: 👤 TẠO TÀI KHOẢN PREMIUM
        run: |
          Write-Host ""
          Write-Host "👤 ĐANG TẠO TÀI KHOẢN PREMIUM" -ForegroundColor Yellow
          
          function New-PremiumPassword {
              param()
              $upper='ABCDEFGHJKLMNPQRSTUVWXYZ'; $lower='abcdefghijkmnpqrstuvwxyz'; $numbers='23456789'; $all=$upper+$lower+$numbers
              $pw=''; $pw+=$upper[(Get-Random -Minimum 0 -Maximum $upper.Length)]
              $pw+=$lower[(Get-Random -Minimum 0 -Maximum $lower.Length)]
              $pw+=$numbers[(Get-Random -Minimum 0 -Maximum $numbers.Length)]
              for($i=1; $i -le 5; $i++){$pw+=$all[(Get-Random -Minimum 0 -Maximum $all.Length)]}
              return -join ($pw.ToCharArray() | Sort-Object { Get-Random })
          }

          if($env:CUSTOM_RDP_PASS){$matKhau=$env:CUSTOM_RDP_PASS; Write-Host "│ ℹ️  Dùng mật khẩu tùy chỉnh từ env" -ForegroundColor Blue}
          else{$matKhau=New-PremiumPassword}

          $securePass=ConvertTo-SecureString $matKhau -AsPlainText -Force

          try{Remove-LocalUser -Name "LongDay2203" -ErrorAction SilentlyContinue; Write-Host "│ ✅ Đã xóa user cũ" -ForegroundColor Green}catch{}

          try{
              New-LocalUser -Name "LongDay2203" -Password $securePass -AccountNeverExpires -Description "LongDay Premium User"
              Enable-LocalUser -Name "LongDay2203"
              Add-LocalGroupMember -Group "Administrators" -Member "LongDay2203"
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member "LongDay2203"
              Write-Host "│ ✅ Đã tạo user + cấp quyền" -ForegroundColor Green
          } catch{
              Write-Host "│ ❌ Lỗi tạo user: $($_.Exception.Message)" -ForegroundColor Red
              exit 1
          }

          echo "RDP_USER=LongDay2203" >> $env:GITHUB_ENV
          echo "RDP_PASS=$matKhau" >> $env:GITHUB_ENV
          echo "$matKhau" > $env:TEMP\rdp_password.txt
          
          Write-Host "✅ Tài khoản LongDay2203 đã sẵn sàng" -ForegroundColor Green

      # ---------------------------
      # KIỂM TRA QUYỀN
      # ---------------------------
      - name: 🔍 KIỂM TRA QUYỀN HỆ THỐNG
        run: |
          Write-Host ""
          Write-Host "🔍 KIỂM TRA QUYỀN ĐĂNG NHẬP" -ForegroundColor Yellow
          
          $user = Get-LocalUser -Name "LongDay2203" -ErrorAction SilentlyContinue
          if($user){Write-Host "│ ✅ User LongDay2203 tồn tại" -ForegroundColor Green}else{Write-Host "│ ❌ User không tồn tại" -ForegroundColor Red; exit 1}
          
          $isAdmin=(Get-LocalGroupMember -Group "Administrators" | Where-Object {$_.Name -like "*LongDay2203"}).Count -gt 0
          if($isAdmin){Write-Host "│ ✅ Có quyền Administrator" -ForegroundColor Green}
          
          $isRDPUser=(Get-LocalGroupMember -Group "Remote Desktop Users" | Where-Object {$_.Name -like "*LongDay2203"}).Count -gt 0
          if($isRDPUser){Write-Host "│ ✅ Có quyền Remote Desktop" -ForegroundColor Green}
          
          $termService=Get-Service -Name TermService
          if($termService.Status -eq 'Running'){Write-Host "│ ✅ Dịch vụ TermService đang chạy" -ForegroundColor Green}
          Write-Host "🎯 Kiểm tra hoàn tất!" -ForegroundColor Green

      # ---------------------------
      # TAILSCALE
      # ---------------------------
      - name: 🌐 THIẾT LẬP MẠNG PREMIUM
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Write-Host ""
          Write-Host "🌐 ĐANG THIẾT LẬP KẾT NỐI MẠNG" -ForegroundColor Yellow
          
          try{
              $msiPath="$env:TEMP\tailscale-premium.msi"
              Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
              Start-Process msiexec.exe -ArgumentList "/i","`"$msiPath`"","/quiet","/norestart" -Wait
              Remove-Item $msiPath -Force
              Write-Host "│ ✅ Cài đặt Tailscale thành công" -ForegroundColor Green
          } catch{
              Write-Host "│ ❌ Lỗi cài đặt Tailscale" -ForegroundColor Red
          }

          Start-Sleep -Seconds 8
          try{
              & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=LongDay-$env:GITHUB_RUN_ID --accept-routes --accept-dns --reset
              Write-Host "│ ✅ Kết nối Tailscale thành công" -ForegroundColor Green
          } catch{
              Write-Host "│ ❌ Lỗi kết nối Tailscale" -ForegroundColor Red
          }

          $ip=$null
          for($i=0;$i -lt 20;$i++){
              try{$ip=& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4;if($ip -match '^\d+\.\d+\.\d+\.\d+$'){echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV; Write-Host "│ ✅ Địa chỉ IP: $ip" -ForegroundColor Green; break}}catch{}
              Start-Sleep -Seconds 3
          }
          if(-not $ip){echo "TAILSCALE_IP=localhost" >> $env:GITHUB_ENV; Write-Host "⚠️ Không thể lấy IP Tailscale" -ForegroundColor Yellow}

      # ---------------------------
      # THÔNG TIN KẾT NỐI
      # ---------------------------
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          $matKhau=Get-Content $env:TEMP\rdp_password.txt
          $durationInput="${{ github.event.inputs.duration }}"
          switch($durationInput){
              "1h" {$totalMinutes=60; $displayTime="1 giờ"}
              "3h" {$totalMinutes=180; $displayTime="3 giờ"}
              "5h40m" {$totalMinutes=340; $displayTime="5 giờ 40 phút"}
              default {$totalMinutes=340; $displayTime="5 giờ 40 phút"}
          }

          $timeZone=[System.TimeZoneInfo]::FindSystemTimeZoneById("SE Asia Standard Time")
          $start=[System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(),$timeZone)
          $end=$start.AddMinutes($totalMinutes)

          Write-Host ""
          Write-Host "┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 KẾT NỐI THÀNH CÔNG!              │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  Địa chỉ: $env:TAILSCALE_IP" -ForegroundColor White
          Write-Host "│ 👤  Tài khoản: LongDay2203" -ForegroundColor White
          Write-Host "│ 🔐  Mật khẩu: $matKhau" -ForegroundColor White
          Write-Host "│ ⏰  Thời lượng: $displayTime" -ForegroundColor White
          Write-Host "│ 🕐  Bắt đầu: $($start.ToString('HH:mm:ss'))" -ForegroundColor White
          Write-Host "│ 🕐  Kết thúc: $($end.ToString('HH:mm:ss'))" -ForegroundColor White
          Write-Host "│ 📍  Port: 3389" -ForegroundColor White
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 💡  Hướng dẫn:                                    │" -ForegroundColor Cyan
          Write-Host "│   1. Mở Remote Desktop Connection                 │" -ForegroundColor Yellow
          Write-Host "│   2. Nhập: $env:TAILSCALE_IP                      │" -ForegroundColor Yellow
          Write-Host "│   3. User: LongDay2203 | Pass: phía trên          │" -ForegroundColor Yellow
          Write-Host "│   4. Enjoy! 🚀                                    │" -ForegroundColor Yellow
          Write-Host "└───────────────────────────────────────────┘" -ForegroundColor Cyan

          echo "TOTAL_MINUTES=$totalMinutes" >> $env:GITHUB_ENV

      # ---------------------------
      # DUY TRÌ PHIÊN
      # ---------------------------
      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $totalMinutes=[int]$env:TOTAL_MINUTES
          $timeZone=[System.TimeZoneInfo]::FindSystemTimeZoneById("SE Asia Standard Time")
          $start=[System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(),$timeZone)
          $end=$start.AddMinutes($totalMinutes)

          Write-Host ""
          Write-Host "🔄 BẮT ĐẦU DUY TRÌ PHIÊN..." -ForegroundColor Yellow
          Write-Host "🔗 Kết nối: $env:TAILSCALE_IP" -ForegroundColor Cyan
          Write-Host "🕐 Bắt đầu: $($start.ToString('HH:mm:ss'))" -ForegroundColor Green
          Write-Host "🕐 Kết thúc: $($end.ToString('HH:mm:ss'))" -ForegroundColor Green
          Write-Host ""

          do{
              $now=[System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(),$timeZone)
              $remain=$end-$now
              $h=[math]::Max(0,[math]::Floor($remain.TotalHours))
              $m=[math]::Max(0,[math]::Floor($remain.TotalMinutes%60))
              $s=[math]::Max(0,[math]::Floor($remain.TotalSeconds%60))
              Write-Host "`r⏳ Thời gian còn lại: $h giờ $m phút $s giây" -NoNewline -ForegroundColor Cyan
              Start-Sleep -Seconds 5
          } while($remain.TotalSeconds -gt 0)

          Write-Host ""
          Write-Host "🎯 HẾT THỜI GIAN — TỰ ĐỘNG DỪNG!" -ForegroundColor Red

      # ---------------------------
      # DỌN DẸP
      # ---------------------------
      - name: 🧹 DỌN DẸP HỆ THỐNG
        if: always()
        run: |
          Write-Host ""
          Write-Host "🧹 ĐANG DỌN DẸP HỆ THỐNG..." -ForegroundColor Yellow
          
          $steps=@(
              @{Name="Xóa file password"; Script={Remove-Item "$env:TEMP\rdp_password
